<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyGithub" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorOnPrimary">@color/mint</item>
        <item name="colorOnSecondaryContainer">@color/teal</item>
        <item name="colorSecondary">?attr/colorPrimary</item>
        <item name="colorOnSecondary">@color/mint_light</item>

    </style>

    <style name="Theme.MyGithub" parent="Base.Theme.MyGithub" />

    <style name="TabLayout" parent="">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/poppins_semibold</item>
        <item name="android:textColor">?attr/colorSecondary</item>
    </style>
</resources>